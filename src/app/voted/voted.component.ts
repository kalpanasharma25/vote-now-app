import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({ templateUrl: 'voted.component.html' })
export class VotedComponent {
    
    constructor(private router: Router) { }
    ngOnInit() {
        };
}